# Minimal DIRECT deps for building the FP8 TRT expert engine (Python 3.12).
# Transitive closure is pinned in scripts/trt-build.lock.txt (uv pip compile).
# torch/torchvision resolve from the CUDA 13 wheel index.
# No flash-attn: the build loads the model with sdpa/eager attention, so the
# flash-attn source compile is not needed (it is a known machine-freeze risk).
torch==2.12.0
torchvision==0.27.0
transformers==4.57.1
einops==0.8.1
hydra-core==1.3.2
scipy==1.16.3
numpy==2.3.5
onnx==1.21.0
ml_dtypes==0.5.4
nvidia-modelopt==0.43.0
tensorrt==11.0.0.114
physical-ai-av==0.2.0
